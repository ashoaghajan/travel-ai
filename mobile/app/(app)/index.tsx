import { PlannerScreen } from '../../src/features/planner/PlannerScreen';

/**
 * Home — the planner, as on the web where "Home" is the planner dashboard.
 *
 * The screen lives under `src/features/` so the route file stays a route and
 * the component stays testable without a router.
 */
export default PlannerScreen;
